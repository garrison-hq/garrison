// Default render for the @panel parallel slot. Returns null when
// no intercepted route is active so the slot adds nothing to the
// shell.
export default function PanelDefault() {
  return null;
}
