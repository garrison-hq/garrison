# M5.2 tasks — CEO chat dashboard surface (read-only)

**Branch**: `011-m5-2-ceo-chat-frontend` | **Plan**: [plan.md](./plan.md) | **Spec**: [spec.md](./spec.md) | **Context**: [m5-2-context.md](../_context/m5-2-context.md) | **M5.1 retro**: [m5-1.md](../../docs/retros/m5-1.md)

19 tasks total, executed linearly by a solo operator. Each task is one Claude Code session in scope and produces a reviewable commit. The repo is in a working state after every task.

---

## Ordering principle

M5.2 extends a shipped M5.1 substrate. Foundation lands first (schema delta + sqlc queries) so both the supervisor sweep extension (T002) and the dashboard server actions (T003) compile against the new column + cascade. After the supervisor side closes (T002), dashboard work goes data-layer first (T003–T005), then UI shell (T006), then per-pane components in dependency order (T007 sidebar, T008 thread header, T009 message stream, T010 composer, T011 empty states). Architecture amendment (T012) lands once the surface compiles end-to-end. Playwright integration tests (T013–T015) boot the full M5.1 + M5.2 stack via the new `m5-2-chat-golden-path.spec.ts` harness — the deferred-from-M5.1 T020 deliverable. Responsive (T016) and chaos (T017) follow. Acceptance (T018) is the scripted SC walk; retro (T019) is the last task.

Zero new runtime dependencies. One approved dev dependency lands during T015 (`@axe-core/playwright`, per resolved Q1 in plan §Phase 5).

Task IDs use three-digit zero-padded form (`T001` … `T019`) matching the M5.1 milestone precedent.

---

## Phase 1 — Foundation (schema + sqlc)

- [ ] T001 Goose migration `20260429000016_m5_2_chat_archive_and_cascade.sql` — add `chat_sessions.is_archived`, recreate `chat_messages.session_id` FK with `ON DELETE CASCADE`, grant DELETE; add new sqlc queries; Drizzle introspection regenerates `schema.supervisor.ts`
  - **Depends on**: M5.1 shipped (`8750831` on main)
  - **Files**: `migrations/20260429000016_m5_2_chat_archive_and_cascade.sql` (new), `migrations/queries/chat.sql` (extend), `supervisor/internal/store/chat.sql.go` (regenerated via sqlc), `dashboard/drizzle/schema.supervisor.ts` (regenerated via `bun run drizzle:pull`), `dashboard/drizzle/_introspected/` (regenerated meta)
  - **Completion condition**: `goose up` against a fresh testcontainer Postgres applies cleanly; `\d+ chat_sessions` shows the new `is_archived BOOLEAN NOT NULL DEFAULT false` column; `\d+ chat_messages` shows the recreated FK with `ON DELETE CASCADE`; `\dp chat_sessions` confirms `garrison_dashboard_app` has `INSERT, SELECT, UPDATE, DELETE` privileges; `goose down` reverses cleanly. Three new sqlc queries land in `migrations/queries/chat.sql` (`FindOrphanedOperatorSessions :many`, `InsertSyntheticAssistantAborted :exec`, `MarkSessionAborted :exec`); before drafting the queries, verify whether `MarkSessionAborted` already exists (the M5.1 `AbortSessionsWithAbortedMessages` query takes a different shape and is NOT a drop-in substitute) — if absent, add the new query; if present in a reusable shape, document the reuse decision in the task commit message. `sqlc generate` regenerates `internal/store/chat.sql.go` cleanly. The M5.1 `MarkPendingMessagesAborted` query is unchanged. `bun run drizzle:pull` regenerates `dashboard/drizzle/schema.supervisor.ts` to add `isArchived: boolean`. `bun run typecheck` passes. **Non-empty-table FK recreate verification**: a manual psql test seeds 5 `chat_messages` rows referencing 1 `chat_sessions` row, runs `goose up`, asserts no rows lost and the new cascade works (`DELETE` on the parent removes the children). Closes SC-219; pins migration shape per plan §1.12.
  - **Out of scope for this task**: any application-code changes (the supervisor sweep extension is T002; the dashboard server actions are T003). No new tables. No `vault_access_log` grant changes (FR-236: no DELETE grant added). No new pg_notify channels in this task.

---

## Phase 2 — Supervisor extension (orphan-row sweep)

- [ ] T002 Extend `RunRestartSweep` in `internal/chat/listener.go` with the orphan-operator-row pass + tests
  - **Depends on**: [T001](#t001)
  - **Files**: `supervisor/internal/chat/listener.go` (extend `RunRestartSweep`), `supervisor/internal/chat/listener_test.go` (extend with orphan-sweep tests), `supervisor/internal/store/chat_orphan_test.go` (new — sqlc query unit tests)
  - **Completion condition**: `RunRestartSweep` runs a second pass after the existing M5.1 pending-message pass: queries `FindOrphanedOperatorSessions` with a 60s cutoff, and for each match opens a tx under `context.WithoutCancel(ctx)` + `TerminalWriteGrace` (concurrency rule 6) that calls `InsertSyntheticAssistantAborted` (turn_index = orphan_turn_index+1, status='aborted', error_kind='supervisor_restart', content='[supervisor restarted before this turn could complete]', terminated_at=NOW()) followed by `MarkSessionAborted`. No `pg_notify` emitted (FR-292; resolved Q4). Reuses the existing `ErrorSupervisorRestart` constant from `internal/chat/errorkind.go` (no new error kind). Six new tests pass: `TestRunRestartSweep_OrphanOperatorRowSyntheticAssistant`, `TestRunRestartSweep_OrphanIgnoresRecentOperatorRow`, `TestRunRestartSweep_OrphanIgnoresAssistantPair`, `TestRunRestartSweep_OrphanIgnoresEndedSession`, `TestRunRestartSweep_OrphanWritesUnderWithoutCancel`, `TestRunRestartSweep_BothPassesIndependent`. Five new tests in `chat_orphan_test.go` pass: `TestFindOrphanedOperatorSessionsMatchesExpected`, `TestFindOrphanedOperatorSessionsIgnoresAssistantPair`, `TestFindOrphanedOperatorSessionsRespectsCutoff`, `TestFindOrphanedOperatorSessionsIgnoresMultipleOlderTurns`, `TestInsertSyntheticAssistantAbortedRespectsTurnIndexUnique`. The full M5.1 supervisor test suite continues green. `vaultlog` analyzer passes — no `slog` call accepts a `vault.SecretValue` argument anywhere in the touched code. Closes SC-215.
  - **Out of scope for this task**: any pg_notify emission for the synthetic aborted row (resolved Q4 — no notify in M5.2). Any change to the existing M5.1 pending-message pass (the orphan pass is purely additive). Any change to the idle sweep. Any new error kind. Any dashboard-side rendering of the synthetic row (covered by T011 inline error variants).

---

## Phase 3 — Dashboard data layer

- [ ] T003 Extend `dashboard/lib/actions/chat.ts` with five new server actions + helper; extend `dashboard/lib/queries/chat.ts` with archived filter + MemPalace-call-age helper; add Vitest unit + integration tests against testcontainer Postgres
  - **Depends on**: [T001](#t001)
  - **Files**: `dashboard/lib/actions/chat.ts` (extend), `dashboard/lib/queries/chat.ts` (extend), `dashboard/lib/actions/chat.test.ts` (new), `dashboard/lib/queries/chat.test.ts` (new)
  - **Completion condition**: `actions/chat.ts` exports five new mutating actions matching plan §1.3 signatures: `createEmptyChatSession()`, `endChatSession(sessionId)`, `archiveChatSession(sessionId)`, `unarchiveChatSession(sessionId)`, `deleteChatSession(sessionId)`, plus internal `requireSessionOwner(sessionId)` helper and the user-facing wrapper `getRecentThreadsForCurrentUser(limit?)`. The existing M5.1 `startChatSession` and `sendChatMessage` exports are unchanged. Each mutating action verifies caller ownership before any write; non-owner collapses to `ChatError(SessionNotFound)` per plan §1.3 (no new `ChatErrorKind` value introduced). `endChatSession` no-ops for already-ended sessions; rejects `aborted` with `ChatError(SessionEnded)`. `deleteChatSession` emits `pg_notify('work.chat.session_deleted', {chat_session_id, actor_user_id})` payload — IDs only, no message content (FR-321). `vault_access_log` rows referencing the deleted session via `metadata.chat_session_id` survive (FK is JSONB-deep, not Postgres FK). `queries/chat.ts` extends `listSessionsForUser(userId, options?)` with backward-compatible `{archived?: boolean, limit?: number}` shape (default `archived=false, limit=50`); extends `getSessionWithMessages(sessionId, options?)` with `{limit?, beforeTurnIndex?}` for pagination per plan §1.8 (returns `{session, messages, hasMore}`); adds `getMostRecentMempalaceCallAge(sessionId)` returning `{ageMs: number | null}` parsed from the most recent assistant `chat_messages.raw_event_envelope` per plan §1.4 + §1.10. The existing M5.1 `getRunningCost` is unchanged. Twelve action tests pass: `TestEndChatSessionMarksEnded`, `TestEndChatSessionRejectsForeignSession`, `TestEndChatSessionIdempotentForEnded`, `TestEndChatSessionRejectsAborted`, `TestArchiveChatSessionFlipsFlag`, `TestUnarchiveChatSessionFlipsFlag`, `TestDeleteChatSessionRemovesRow`, `TestDeleteChatSessionEmitsNotify`, `TestDeleteChatSessionPreservesVaultLog`, `TestCreateEmptyChatSessionInsertsRowOnly`, `TestCreateEmptyChatSessionRapidCalls`, `TestRequireSessionOwnerCollapsesNonOwnerToNotFound`. Seven query tests pass: `TestListSessionsForUserFiltersArchivedDefault`, `TestListSessionsForUserCanIncludeArchived`, `TestGetSessionWithMessagesPaginates`, `TestGetSessionWithMessagesLoadsEarlier`, `TestGetMostRecentMempalaceCallAgeReturnsAge`, `TestGetMostRecentMempalaceCallAgeReturnsNullForNone`, `TestGetMostRecentMempalaceCallAgeIgnoresFailedCalls`. `bun run typecheck` and `bun run lint` pass. Zero new runtime dependencies in `dashboard/package.json`.
  - **Out of scope for this task**: any UI component (T006–T011 own those). Any SSE consumer hook (T005). Any activity-feed channel addition (T004). Any new `ChatErrorKind` enum value (collapsed by plan §1.3). Any thread-number computation or display (T008 owns the per-operator counter render). Any direct DELETE grant work — that landed in T001.

- [ ] T004 Activity-feed integration: add `work.chat.session_deleted` literal channel + new `ActivityEvent` variant + `EventRow` rendering branch
  - **Depends on**: [T003](#t003)
  - **Files**: `dashboard/lib/sse/channels.ts` (extend `KNOWN_CHANNELS` + `parseChannel`), `dashboard/lib/sse/events.ts` (extend `ActivityEvent` discriminated union), `dashboard/lib/sse/channels.test.ts` (extend), `dashboard/components/features/activity-feed/EventRow.tsx` (extend), plus a chat-flavoured icon if `dashboard/components/ui/icons.tsx` doesn't already carry one (re-use `ChatIcon` if introduced here; otherwise T007 introduces it and T004 imports the alias)
  - **Completion condition**: `KNOWN_CHANNELS` includes `'work.chat.session_deleted'` as a literal entry. `parseChannel` adds a branch returning `{kind: 'chat.session_deleted', eventId, at, chatSessionId, actorUserId}`. `ActivityEvent` discriminated union gains the new variant. `EventRow.tsx` adds a render branch following the M3 audit-event design language (icon + actor + concise predicate + relative time per amended FR-322): chat icon + literal copy "Chat thread <chatSessionId-short> deleted by operator" + relative-time stamp. Three new tests pass: `TestSessionDeletedChannelKnown`, `TestSessionDeletedParsesPayload`, `TestSessionDeletedRejectsExtraneousFields` (Rule 6 backstop — message content keys silently dropped from any malformed payload). The existing activity feed unit + integration tests stay green. Closes FR-320 + FR-322.
  - **Out of scope for this task**: any change to the M5.1 chat channels (`session_started`, `message_sent`, `session_ended` are unchanged). Any `EventRow` rendering for the existing M5.1 chat channels — that's deferred operator-tooling polish per amended FR-322 and is NOT in M5.2 scope. Any pattern-channel additions (this is a literal-only channel; no `KNOWN_CHANNEL_PATTERNS` change). Any rendering of agent-finalize-shaped chat events (M5.3+).

---

## Phase 4 — Dashboard SSE consumer hook

- [ ] T005 `dashboard/lib/sse/chatStream.ts` — `useChatStream(sessionId)` hook implementing the 5-state listener pattern + dedupe + reconnect-preserves-partial; tests with mock `EventSource`
  - **Depends on**: [T003](#t003)
  - **Files**: `dashboard/lib/sse/chatStream.ts` (new), `dashboard/lib/sse/chatStream.test.ts` (new)
  - **Completion condition**: `useChatStream(sessionId)` returns `{state, partialDeltas: Map<messageId, string>, terminals: Map<messageId, ChatTerminalEvent>, sessionEnded: boolean, lastError: string | null}`. State machine matches plan §1.5 lifecycle diagram (`dormant/connecting/live/backoff/idle-grace`). Dedupe uses a private `Set<string>` of `(messageId, seq)` keys; double-`delta` events at the same seq do not double-render. Mid-stream disconnect transitions to `backoff` and preserves `partialDeltas` already accumulated; on reconnect the consumer relies on the M5.1 SSE route's row-state-read mechanism (per amended FR-261) to receive any committed terminal it missed; partial deltas are NOT replayed. The hook does not send `Last-Event-ID` headers manually (the browser does so automatically; the server doesn't read them). On `terminal` event, the message's terminal state is set in `terminals` map (partial buffer remains; renderer prefers terminal). On `sessionId` prop change, the previous `EventSource` is closed cleanly before opening the new one (FR-232/FR-263). On hook unmount, enters `idle-grace` for 60s (matches M3 listener semantics); re-subscription within window skips reconnect. Backoff schedule: `100ms → 200 → 400 → … → 30000ms` cap. Eight tests pass: `TestChatStreamAccumulatesDeltas`, `TestChatStreamDedupesOnSeq`, `TestChatStreamPreservesPartialOnReconnect`, `TestChatStreamFinalisesOnTerminal`, `TestChatStreamClosesOnSessionEnded`, `TestChatStreamClosesOnSessionIdChange`, `TestChatStreamIdleGraceOnUnmount`, `TestChatStreamBackoffSchedule`. The hook is the chat-specific instance per plan §R1; the M3 `lib/sse/listener.ts` singleton is **not** modified. `bun run typecheck` and `bun run lint` pass. Closes the SSE consumer side of FR-260 / FR-261 (per amended mechanism description) / FR-262 / FR-263 / FR-264.
  - **Out of scope for this task**: any modification to `lib/sse/listener.ts` (the M3 singleton stays untouched per plan §R1 — DRY-ing the two listeners is explicitly rejected). Any change to the M5.1 SSE route at `app/api/sse/chat/route.ts` (the route is the producer; this hook is the consumer; route is unchanged). Any rendering or component glue (T007–T010 consume the hook). Any direct integration test against a real SSE route (T013 covers that).

---

## Phase 5 — Chat surface UI

- [ ] T006 Chat route shell — three-pane layout, route entries, `ChatShell`, `ChatTopbarStrip`, `KnowsPanePlaceholder`; renders end-to-end with stub thread data
  - **Depends on**: [T005](#t005)
  - **Files**: `dashboard/app/[locale]/(app)/chat/layout.tsx` (new), `dashboard/app/[locale]/(app)/chat/page.tsx` (new), `dashboard/app/[locale]/(app)/chat/[[...sessionId]]/page.tsx` (new), `dashboard/app/[locale]/(app)/chat/all/page.tsx` (new), `dashboard/components/features/ceo-chat/ChatShell.tsx` (new), `dashboard/components/features/ceo-chat/ChatTopbarStrip.tsx` (new), `dashboard/components/features/ceo-chat/KnowsPanePlaceholder.tsx` (new)
  - **Completion condition**: navigating to `/chat` renders the three-pane layout (left rail unchanged from M3 sidebar; center pane stub "Pick or create a thread" empty state; right pane `KnowsPanePlaceholder` with copy "**What the CEO knows** — Knowledge-base context lands in M5.4. For now this pane is reserved."). Navigating to `/chat/<seeded-uuid>` renders the same three-pane layout with the center pane showing the thread title (stubbed `thread #N` from the seeded session) and a placeholder message stream + composer body (component implementations land in T008–T010). Navigating to `/chat/all` renders the full thread-list page (server-rendered table; archived tab stub). Three-pane responsive at ≥768/1024/1280 (`<1024px` collapses right pane to a header strip). The chat layout adds its own topbar strip via `ChatTopbarStrip` (top of center pane: "+ New thread" button right-aligned, breadcrumb left-aligned, idle pill stub center-right) **without modifying the global `Topbar.tsx`** (resolved Q2 — two strips). The "+ New thread" button calls `createEmptyChatSession` (T003) and routes to `/chat/<new-uuid>`. `bun run typecheck` and `bun run lint` pass. The route compiles; smoke-test against the dev server passes (route lands without 500s).
  - **Out of scope for this task**: any thread-history subnav in the sidebar (T007). Any thread header content beyond a stubbed title (T008). Any actual message stream or composer rendering (T009 + T010). Any ConfirmDialog wiring (T008). Any axe-core a11y assertion (T015). Any breaking change to `(app)/layout.tsx` (the chat layout composes inside it).

- [ ] T007 Sidebar "CEO chat" entry + `ThreadHistorySubnav` + recent-threads server action wire-up
  - **Depends on**: [T006](#t006)
  - **Files**: `dashboard/components/layout/Sidebar.tsx` (extend), `dashboard/components/features/ceo-chat/ThreadHistorySubnav.tsx` (new client component), `dashboard/components/ui/icons.tsx` (extend with `ChatIcon` if not introduced in T004), plus an `en.json` translation key only if absolutely required (literal-string posture per plan §1.18 — prefer literal `"CEO chat"` to avoid M5.2 i18n churn)
  - **Completion condition**: `Sidebar.tsx` renders a new "CEO chat" `NavLink` between `Activity` and `Hygiene` with the `ChatIcon` thin-line speech-bubble. Below the link, a client `<ThreadHistorySubnav />` collapsible (using native `<details>`/`<summary>`) lists the latest 10 active threads from `getRecentThreadsForCurrentUser(10)` (T003), ordered `started_at DESC`, filtered to `is_archived=false`. Each thread row links to `/chat/<uuid>` with the per-operator-counter title `thread #N` (computed at query time per plan §1.11). A "view all" link routes to `/chat/all`. Active-state indicator on the "CEO chat" entry when on the chat route (filled icon + accent text). The subnav is keyboard-accessible (Enter/Space toggles `<details>`; Tab traverses thread rows; Enter activates a row). On `<768px` the subnav collapses behind the existing M3 sidebar collapse pattern. The `Hiring` and `Skills` entries are NOT added (FR-206/207). **The existing M3 live-agents widget at `Sidebar.tsx:99-112` MUST remain byte-unchanged** in this commit per FR-208 — the diff for `Sidebar.tsx` shows only additions (the new `NavLink` + the subnav slot), zero changes to the live-agents widget block. Existing M3 sidebar tests stay green; one new test `TestThreadHistorySubnavRendersUserOwnedThreads` covers the data binding. `bun run typecheck` and `bun run lint` pass. Closes FR-203 / FR-204 / FR-206 / FR-207 / FR-208 / FR-230 / FR-231.
  - **Out of scope for this task**: any modification to the global Topbar (resolved Q2). Any modification to `Sidebar.tsx:99-112` (FR-208 byte-unchanged invariant). Any per-thread overflow menu in the subnav (overflow lives in the thread header per FR-211). Any SSE-driven subnav refresh (subnav refetches on route navigation; live updates are not in scope). Any rename UX (FR-211 omits in v1 per plan §1.11 + slate item 28). Any "all systems nominal" topbar chip (FR-253; out of scope).

- [ ] T008 `ThreadHeader` + `ThreadOverflowMenu` + `IdlePill` + `CostBadge` + `format.ts` helpers + tests
  - **Depends on**: [T006](#t006), [T007](#t007)
  - **Files**: `dashboard/components/features/ceo-chat/ThreadHeader.tsx` (new), `dashboard/components/features/ceo-chat/ThreadOverflowMenu.tsx` (new), `dashboard/components/features/ceo-chat/IdlePill.tsx` (new), `dashboard/components/features/ceo-chat/format.ts` (new — cost / time-ago / thread-number helpers), `dashboard/components/features/ceo-chat/ThreadHeader.test.tsx` (new), `dashboard/components/features/ceo-chat/IdlePill.test.tsx` (new)
  - **Completion condition**: `ThreadHeader` renders the per-operator `thread #N` title (from query-time counter), `started <relative-time>`, `<turn_count> turns`, and `<CostBadge value={session.totalCostUsd} precision={2} />` formatted via `Intl.NumberFormat('en-US', {style: 'currency', currency: 'USD', minimumFractionDigits: 2, maximumFractionDigits: 2})` in `font-mono font-tabular`. `ThreadOverflowMenu` renders the spec-pinned items per FR-211: "Archive thread" (always visible), "Unarchive thread" (visible only when `is_archived=true`), "End thread" (visible only when `status='active'`), "Delete thread" (always visible). No "Rename" item in v1 (resolved per slate item 28 + plan §1.11). Each menu item triggers the corresponding T003 server action; "End thread" + "Delete thread" open `<ConfirmDialog tier='single-click'>` with the spec-pinned copy from FR-237 + FR-245. Idle pill renders inside `ChatTopbarStrip` via `<IdlePill status={session.status} />` (slot wired by T006; this task implements the component): `active`→`tone='ok'` "active" with `pulse=true`, `ended`→`tone='warn'` "idle", `aborted`→`tone='err'` "aborted". Color + label combination satisfies FR-332. `format.ts` exports: `formatThreadTitle(threadNumber: number)`, `formatTimeAgo(date: Date)`, `formatSessionCost(usd: number)`, `formatPerMessageCost(usd: number | null)`. Six new tests pass: `TestThreadHeaderRendersThreadNumber`, `TestThreadHeaderRendersCostBadge`, `TestThreadHeaderOverflowMenuShowsEndOnlyForActive`, `TestThreadHeaderOverflowMenuShowsUnarchiveOnlyForArchived`, `TestThreadHeaderRenameItemAbsent`, `TestThreadHeaderDeleteOpensConfirmDialog`. Three IdlePill tests pass: `TestIdlePillRendersGreenForActive`, `TestIdlePillRendersYellowForEnded`, `TestIdlePillRendersRedForAborted`. `bun run typecheck` and `bun run lint` pass. Closes FR-210 / FR-211 / FR-212 (button placement) / FR-237 / FR-245 / FR-250 / FR-251 / FR-332 (status indicators). Component-level coverage of SC-203 / SC-204 / SC-205 (Playwright e2e in T014 closes the SCs).
  - **Out of scope for this task**: any actual end/archive/delete server-action invocation through Playwright (T014 covers that). Any dropping of the `claude_session_label` column or rename UX wiring (deferred per plan §1.11). Any cost-cap-reached display in the header (handled in MessageStream T009 / EmptyStates T011). Any palace-live chip work (T010).

- [ ] T009 `MessageStream` + `MessageBubble` + sticky-bottom hook + cursor + per-message cost footer + a11y `aria-live` + tests
  - **Depends on**: [T005](#t005), [T008](#t008)
  - **Files**: `dashboard/components/features/ceo-chat/MessageStream.tsx` (new), `dashboard/components/features/ceo-chat/MessageBubble.tsx` (new), `dashboard/components/features/ceo-chat/useStickyBottom.ts` (new — hook), `dashboard/components/features/ceo-chat/MessageStream.test.tsx` (new), plus a CSS keyframe `garrison-cursor-blink` added to `dashboard/app/globals.css`
  - **Completion condition**: `MessageStream` is a client component that consumes `useChatStream(sessionId)` (T005) and the server-rendered initial message list, rendering each `chat_messages` row in `(turn_index, role)` order. Operator messages right-aligned in a darker bubble (matching M3 design language); assistant messages left-aligned with a `C` initial-avatar header + `CEO · <model>` badge (`<model>` derived from the most recent `raw_event_envelope.model` for that turn — cosmetic display only per FR-225, click is a no-op). Per-message cost in assistant bubble footer at 4 decimals via `formatPerMessageCost` from T008; operator bubbles render no footer (cost is `NULL`). Streaming UX: in-flight assistant bubble appends-as-arrived from `partialDeltas.get(messageId)`; trailing `▍` cursor (ARIA-hidden) renders while the message has no terminal entry; `garrison-cursor-blink` keyframe animates 1.06s; `prefers-reduced-motion: reduce` swaps to steady cursor (no animation). `aria-live="polite"` + `aria-busy={status === 'streaming'}` on the in-flight bubble container; flips to `aria-busy="false"` on terminal commit. Operator + completed-assistant bubbles render in non-live wrappers so transcript reload doesn't re-announce (FR-331). Sticky-bottom: `useStickyBottom` hook tracks `scrollTop` vs `scrollHeight - clientHeight`; auto-scrolls during streaming when within 40px of bottom; if operator scrolls up >100px, auto-scroll disables and a "↓ N new" pill appears (counts terminal commits since scroll-up); clicking the pill scrolls to bottom and re-engages stickiness. Pagination per plan §1.8: server-rendered initial fetch returns top 50 turns; client renders a "Load earlier" button at the top when `hasMore=true`; click refetches with `beforeTurnIndex = oldest currently rendered`. Six tests pass: `TestMessageStreamRendersTurnIndexOrder`, `TestMessageStreamShowsCursorWhileStreaming`, `TestMessageStreamHidesCursorAfterTerminal`, `TestMessageStreamShowsNewPillOnScrollUp`, `TestMessageStreamRespectsReducedMotion`, `TestMessageStreamLoadsMoreOnScrollToTop`. `bun run typecheck` and `bun run lint` pass. Closes FR-220 / FR-221 / FR-222 / FR-223 / FR-224 / FR-225 / FR-226 / FR-331.
  - **Out of scope for this task**: any composer wiring (T010). Any inline error variant rendering for `ChatErrorKind` cases (T011 closes that). Any palace-live chip work (T010). Any `@tanstack/react-virtual` integration — pagination is the M5.2 choice per plan §1.8 (the dependency is left available for future M5.4 polish). Any tool-call inline chip surface (deferred to v1.1 per spec out-of-scope).

- [ ] T010 `Composer` + `PalaceLiveChip` + state machine + send/⌘↵ + max-input + auto-focus + a11y + tests
  - **Depends on**: [T009](#t009)
  - **Files**: `dashboard/components/features/ceo-chat/Composer.tsx` (new), `dashboard/components/features/ceo-chat/PalaceLiveChip.tsx` (new), `dashboard/components/features/ceo-chat/Composer.test.tsx` (new), `dashboard/components/features/ceo-chat/PalaceLiveChip.test.tsx` (new), plus a tiny non-blocking-toast helper if the dashboard doesn't already carry one (preferred: reuse an existing M3/M4 toast primitive — verify at task-start)
  - **Completion condition**: `Composer` is a client component implementing the state machine from plan §1.6: `idle | typing | submitting | streaming | disabled-ended | disabled-cost-cap`. `aborted` is NOT a disabled state (composer stays enabled per FR-215 — operator's next turn proceeds as a new message in the same session). Multi-line `<textarea>` with footer carrying `<PalaceLiveChip ageMs={…} />` + send button labelled "send" with `<Kbd>⌘↵</Kbd>` and explanatory caption "CEO will be summoned when you send. Each message spawns a fresh process." Submit binding: `(event.metaKey || event.ctrlKey) && event.key === 'Enter'` calls `sendChatMessage` (M5.1) or `startChatSession` (M5.1) depending on whether a current session exists. Plain `Enter` inserts newline. Send button disabled for empty / whitespace-only drafts (FR-281). Loading state on send button between submit and SSE first-delta (FR-282). Max input: 10240 bytes (UTF-8); paste exceeding the limit truncates with a non-blocking toast warning. Auto-focus: on session-open via `useEffect`; after each terminal commit (`useEffect` keyed on `terminals` map size). `aria-keyshortcuts="Meta+Enter"` on the textarea. PalaceLiveChip: thresholds per FR-283 (≤5min `live`/`ok`, 5-30min `stale`/`warn`, >30min OR null `unavailable`/`err`); `ageMs` prop derived from `getMostRecentMempalaceCallAge(sessionId)` (T003) refetched on every terminal commit. StatusDot + label combination satisfies FR-332. Nine Composer tests pass: `TestComposerSubmitsOnCmdEnter`, `TestComposerInsertsNewlineOnEnter`, `TestComposerDisablesOnEndedSession`, `TestComposerStaysEnabledOnAbortedSession`, `TestComposerDisablesDuringStreaming`, `TestComposerRejectsOversizePaste`, `TestComposerSendButtonDisabledForEmpty`, `TestComposerSendButtonDisabledForWhitespaceOnly`, `TestComposerAutoFocusesOnMount`. Four PalaceLiveChip tests pass: `TestPalaceLiveChipLiveUnder5Min`, `TestPalaceLiveChipStaleAt15Min`, `TestPalaceLiveChipUnavailableOver30Min`, `TestPalaceLiveChipUnavailableNullAge`. `bun run typecheck` and `bun run lint` pass. Closes FR-213 / FR-214 / FR-215 / FR-216 / FR-217 / FR-218 / FR-280 / FR-281 / FR-282 / FR-283 / FR-332 (composer chip) / FR-333 (`aria-keyshortcuts`).
  - **Out of scope for this task**: any inline error variant rendering (T011). Any model-picker (out of scope per spec; FR-225 cosmetic only). Any tool-call inline chip rendering (deferred to v1.1). Any deep-link to vault edit surface for `vault_token_expired` errors (T011 wires the error table that carries the deep links).

- [ ] T011 `EmptyStates` + `errorMessages.ts` + inline error variant rendering in `MessageBubble` + tests
  - **Depends on**: [T009](#t009), [T010](#t010)
  - **Files**: `dashboard/components/features/ceo-chat/EmptyStates.tsx` (new — three exported components), `dashboard/components/features/ceo-chat/errorMessages.ts` (new — `CHAT_ERROR_DISPLAY` table), `dashboard/components/features/ceo-chat/MessageBubble.tsx` (extend with error-variant branch), `dashboard/components/features/ceo-chat/EmptyStates.test.tsx` (new), `dashboard/components/features/ceo-chat/errorMessages.test.tsx` (new — error-table mapping coverage)
  - **Completion condition**: `EmptyStates.tsx` exports three components matching plan §1.15: `NoThreadsEverEmptyState({onCreate})`, `EmptyCurrentThreadHint()`, `EndedThreadEmptyState({onCreateNew})`. Copy verbatim from plan §1.15 (no localisation in M5.2 per slate item 42). Each composes the M3 `<EmptyState>` primitive with the appropriate icon + description + caption + action button. `errorMessages.ts` exports `CHAT_ERROR_DISPLAY: Record<string, {headline, body, deepLinkHref?, deepLinkLabel?}>` keyed on every value of the M5.1 `ErrorKind` enum from `internal/chat/errorkind.go` (`session_cost_cap_reached`, `session_ended`, `vault_token_not_found`, `vault_token_expired`, `vault_unavailable`, `claude_oauth_invalid`, `mcp_postgres_unhealthy`, `mcp_mempalace_unhealthy`, `container_crashed`, `subprocess_timeout`, `parser_error`, `supervisor_restart`). `MessageBubble.tsx` renders an error variant when the assistant row's `errorKind` is non-null: error icon + `headline` + `body` + optional `<a href={deepLinkHref}>{deepLinkLabel}</a>` (vault-token-expired deep-links to `/vault/edit/.../CLAUDE_CODE_OAUTH_TOKEN`). The composer (T010) reads the latest assistant row's `errorKind`: `session_cost_cap_reached` flips composer to `disabled-cost-cap`; all other error kinds keep composer enabled (operator's next turn proceeds as a new message per FR-271). `MessageStream` (T009) renders `EndedThreadEmptyState` below the message stream when `session.status='ended'` (FR-270c); does NOT render an empty-state for `aborted` (FR-271 inline error in the failed bubble instead). `NoThreadsEverEmptyState` renders on `/chat` when `listSessionsForUser` returns 0 rows. Five tests pass: `TestNoThreadsEverEmptyStateRendersCTA`, `TestEndedThreadEmptyStateRendersCopy`, `TestErrorMessagesTableCoversAllKinds` (asserts every M5.1 ErrorKind has an entry), `TestMessageBubbleRendersErrorVariant`, `TestVaultTokenExpiredHasDeepLink`. `bun run typecheck` and `bun run lint` pass. Closes FR-270 / FR-271 / FR-272 / FR-273.
  - **Out of scope for this task**: any new `ChatErrorKind` value (the table is keyed on M5.1's frozen vocabulary). Any rename of error kinds in the error table — exact strings match the supervisor enum. Any localisation of error copy (slate item 42; English-only literal strings in M5.2). Any retry-spawn affordance (no "Retry last turn" button in v1 per FR-271).

---

## Phase 6 — Architecture amendment

- [ ] T012 Amend `ARCHITECTURE.md:574` to enumerate M5.1 / M5.2 / M5.3 / M5.4; add unit test asserting the line lands
  - **Depends on**: [T011](#t011)
  - **Files**: `ARCHITECTURE.md` (amend line 574), `dashboard/tests/architecture-amendment.test.ts` (new)
  - **Completion condition**: `ARCHITECTURE.md:574` (or whatever line the M5 entry resolves to post-amendment) reads verbatim per plan §1.18: *"M5 — CEO chat (summoned). Conversation panel, summon-per-message pattern. M5.1 ships the read-only backend (server actions, SSE producer, transcript reads, idle/restart sweeps). M5.2 ships the dashboard surface (three-pane layout, message stream, composer, multi-session UX, end/archive/delete affordances). M5.3 adds chat-driven mutations behind a threat-model amendment. M5.4 ships the "WHAT THE CEO KNOWS" knowledge-base pane."* The amendment is additive — does not remove or revise the existing read-only stance. One unit test passes: `TestArchitectureLineEnumeratesM5Submilestones` (reads `ARCHITECTURE.md`, asserts the M5 entry contains "M5.1", "M5.2", "M5.3", "M5.4" — substring matches, not line-number-pinned, so the test survives line-number drift). `bun run vitest dashboard/tests/architecture-amendment.test.ts` passes. Closes FR-310 / SC-217.
  - **Out of scope for this task**: any other ARCHITECTURE.md edits. Any RATIONALE.md edit. Any AGENTS.md edit. Any change to the existing read-only stance (M5.3 owns that amendment).

---

## Phase 7 — Playwright integration tests (closes M5.1's deferred T020)

- [ ] T013 Playwright golden-path harness scaffolding + sub-scenarios `a` (single-turn) + `b` (multi-turn continuity) — boots the full M5.1 + M5.2 stack
  - **Depends on**: [T011](#t011), [T012](#t012)
  - **Files**: `dashboard/tests/integration/m5-2-chat-golden-path.spec.ts` (new), `dashboard/tests/integration/_chat-harness.ts` (new — extends existing `_harness.ts` with chat-stack boot), `dashboard/tests/integration/_server-coverage.ts` (extend if needed for chat coverage), plus any one-off fixture file under `dashboard/tests/fixtures/`
  - **Completion condition**: `_chat-harness.ts` boots testcontainer Postgres + Infisical + `garrison-mockclaude:m5` chat image + supervisor process + standalone dashboard bundle. The harness is reusable across T013/T014/T015 sub-scenarios. The harness exposes a `setSupervisorEnv({ GARRISON_CHAT_SESSION_IDLE_TIMEOUT?: string })` helper so later sub-scenarios can override the idle timeout for SC-208 verification. Two `test()` blocks pass: `test('a — golden path single turn')` covers User Story 1 — login, navigate to `/chat`, click "+ New thread", land on `/chat/<uuid>`, type a question, press `⌘+Enter`; assert composer disables during streaming, deltas append-as-arrived, cursor visible, per-message cost renders at 4 decimals, per-session cost badge updates at 2 decimals, idle pill stays green/active, both `chat_messages` rows exist in DB, AND the wall-clock from click → first DOM-rendered delta ≤ 5s (closes SC-200). `test('b — multi-turn continuity')` extends `a` — sends turn 2 referencing turn 1's content, asserts the assistant references turn 1 via deixis ("those tickets"), asserts `cache_read_input_tokens > 0` from turn 2's `raw_event_envelope.message_start.usage` (closes M5.1 SC-002 carryover). The chat-image build step is idempotent (cached after first run); CI first-run cost is a one-time infra step. Total runtime for a + b ≤ 60s. `bun run test:integration -- m5-2-chat-golden-path` passes both blocks against `garrison-mockclaude:m5`. Closes SC-201; partial closure of SC-200.
  - **Out of scope for this task**: thread-management sub-scenarios c/d/e (T014). Disconnect / a11y / leak / idle-flip / mid-stream-end / TTI sub-scenarios f-k (T015). Responsive-layout block (T016). Any chaos test work (T017). Any `@axe-core/playwright` dependency add — that lands during T015. Any change to the M5.1 chat-image build (`garrison-mockclaude:m5` reuses M5.1's Dockerfile.mockclaude.chat unchanged).

- [ ] T014 Playwright sub-scenarios `c` (multi-session switching) + `d` (end thread, idle path) + `e` (archive + delete)
  - **Depends on**: [T013](#t013)
  - **Files**: `dashboard/tests/integration/m5-2-chat-golden-path.spec.ts` (extend with three new `test()` blocks)
  - **Completion condition**: `test('c — multi-session switching')` seeds 3 sessions owned by the test operator; navigates to `/chat`; clicks each subnav entry; asserts URL updates to `/chat/<uuid>`, transcript reloads via `getSessionWithMessages`, SSE EventSource for previous session closes cleanly, new EventSource opens; sends a message in session A; switches to session B mid-stream; asserts no delta from A renders in B (closes SC-202). `test('d — end thread (idle path)')` opens an active session in idle state (no in-flight turn); clicks overflow → "End thread"; confirms via single-click `ConfirmDialog`; asserts within 1s `chat_sessions.status='ended'`, composer disables, ended-state EmptyState renders, topbar pill flips green→yellow (closes SC-203 idle-path). The mid-stream end-thread race is covered by sub-scenario `j` in T015. `test('e — archive and delete')` opens a session; clicks overflow → Archive; asserts thread filters out of default subnav, appears in archived sub-view; click Unarchive; asserts thread reappears in default subnav. Click Delete; confirms via single-click dialog; asserts thread vanishes from all left-rail views, `chat_sessions` row gone, `chat_messages` rows cascade-deleted (closes SC-204 + SC-205). Total cumulative runtime for a–e ≤ 4 minutes. `bun run test:integration -- m5-2-chat-golden-path` passes all five blocks (a–e).
  - **Out of scope for this task**: disconnect / a11y / leak / idle-pill flip / mid-stream end / TTI sub-scenarios f-k (T015). Any change to the harness scaffolding (T013 owns it). Any new dependencies (`@axe-core/playwright` is T015). Any modification to the deletion's cascade behavior — the M5.1 schema + T001's FK recreate already handle it.

- [ ] T015 Playwright sub-scenarios `f` (mid-stream disconnect + reconnect) + `g` (axe-core a11y) + `h` (vault-leak grep) + `i` (idle-pill flip after supervisor sweep) + `j` (end-thread mid-stream race) + `k` (long-thread TTI); add `@axe-core/playwright` dev dependency
  - **Depends on**: [T014](#t014)
  - **Files**: `dashboard/tests/integration/m5-2-chat-golden-path.spec.ts` (extend with six new `test()` blocks), `dashboard/package.json` (add `@axe-core/playwright` to `devDependencies`), `dashboard/bun.lock` (regenerated)
  - **Completion condition**: `@axe-core/playwright` lands as a `devDependencies` entry per resolved Q1; commit message names the AGENTS.md soft-rule justification (no stdlib alternative; manual review unsuitable as CI regression guard; ~5MB dev tree, MIT-licensed, official Playwright integration).
    - `test('f — mid-stream disconnect and reconnect')` starts a chat turn; mid-stream calls `await page.context().setOffline(true)`; waits 200ms; calls `setOffline(false)`; asserts listener flips through `live → backoff → connecting → live`, partial accumulated buffer remains (no clear-and-redraw), no delta text duplicated on reconnect, terminal `result` event arrives exactly once, bubble locks with full content (closes SC-209).
    - `test('g — axe-core a11y')` runs `@axe-core/playwright` against `/chat/<uuid>` after the golden-path render lands; asserts zero serious or critical violations on the chat route in golden-path render (closes SC-222). Moderate or minor violations are not gating in M5.2 (matches M3/M4 a11y precedent per FR-334).
    - `test('h — vault-leak parity grep')` seeds a sentinel `sk-test-PLANT-DO-NOT-LEAK-MARKER` value into the test palace via a controlled write; runs a chat turn that reads that palace entry; asserts the sentinel does NOT appear in (a) dashboard server logs, (b) browser console, (c) network tab payloads, (d) any received SSE event payload (closes SC-216 carryover).
    - `test('i — idle-pill flip after supervisor sweep')` boots the harness with `GARRISON_CHAT_SESSION_IDLE_TIMEOUT=10s` (via `setSupervisorEnv` from T013); opens an active session; lands at least one assistant terminal commit (so the session is at active+idle baseline); waits 12s without any operator interaction; polls the `/chat/<uuid>` page DOM and asserts within one page render cycle that the topbar idle pill flipped green→yellow AND `chat_sessions.status` rolled to `'ended'` in DB (closes SC-208).
    - `test('j — end-thread mid-stream race')` starts a chat turn that elicits a long streaming response (mockclaude chat-mode emits ~30 deltas); after at least 3 deltas have arrived but BEFORE the terminal `result` event, clicks overflow → "End thread" + confirms via single-click dialog; asserts `endChatSession` returns immediately (`chat_sessions.status='ended'`), the in-flight assistant turn finishes streaming and commits its terminal row naturally (no truncation), and a follow-up `sendChatMessage` against the now-ended session returns `ChatError(SessionEnded)` / surfaces as `error_kind='session_ended'` in the next bounced message — covers FR-244's race window explicitly.
    - `test('k — long-thread time-to-interactive')` seeds a session with 100 committed turns (alternating operator/assistant); navigates to `/chat/<seeded-uuid>`; measures time-to-interactive via `page.evaluate(() => performance.timing.domContentLoaded - performance.timing.navigationStart)` (or the modern `PerformanceNavigationTiming` equivalent); asserts TTI < 2000ms; asserts the rendered DOM shows the most recent 50 turns (pagination per plan §1.8) plus a "Load earlier" affordance (closes SC-221).
    Total cumulative runtime for a–k ≤ 9 minutes. `bun run test:integration -- m5-2-chat-golden-path` passes all eleven blocks. Full M3 + M4 + M5.1 + M5.2 Playwright suite stays under 12 minutes total in CI (closes M5.1 SC-010 + SC-011 carryover; SC-213).
  - **Out of scope for this task**: any chaos work (T017). Any responsive-layout test (T016). Any moderate or minor axe-core violation fix that does not block (FR-334 — only serious/critical are gating). Any new runtime dependency (only the dev dependency adds; `dashboard/package.json` `dependencies` block stays unchanged).

---

## Phase 8 — Responsive + chaos

- [ ] T016 Add chat surface block to `dashboard/tests/integration/responsive.spec.ts`
  - **Depends on**: [T015](#t015)
  - **Files**: `dashboard/tests/integration/responsive.spec.ts` (extend)
  - **Completion condition**: One new `test('chat surface — 768/1024/1280 layouts')` block iterates the three viewport sizes; for each viewport navigates to `/chat/<seeded-uuid>`; asserts three-pane layout renders without overflow (no horizontal scroll on any pane), sticky-bottom scroll math works, topbar strip stays visible, right pane collapses to a header strip on `<1024px` per FR-202. Existing M3 + M4 responsive blocks stay green. `bun run test:integration -- responsive` passes. Closes SC-210.
  - **Out of scope for this task**: sub-768px work (out of scope per FR-202). Any layout redesign — only assertions, not visual changes. Any axe-core check at non-default viewports.

- [ ] T017 Chaos test: `chaos_m5_1_test.go` real `docker kill` against `garrison-mockclaude:m5` mid-stream (closes M5.1 SC-006 + SC-009 + FR-101)
  - **Depends on**: [T002](#t002)
  - **Files**: `supervisor/internal/chat/chaos_m5_1_test.go` (new — file name per FR-303, even though test lives in M5.2)
  - **Completion condition**: One test passes: `TestChat_DockerKillMidStream` boots testcontainer Postgres + Infisical + the real `garrison-mockclaude:m5` container (NOT the M5.1 canned-NDJSON `fakeDockerExec`); starts a chat session with a long-prompt-that-elicits-long-output prompt (mockclaude chat-mode emits ~30 deltas before terminal); asserts at least 5 deltas have arrived via SSE; executes `docker kill <chat-container-id>` from outside the supervisor; asserts within 5s the container is gone (`docker ps` empty for it), `chat_messages` terminal row committed with `error_kind='container_crashed'`, `chat_sessions.status='aborted'`, the SSE consumer received a typed-error event, AND the next operator INSERT on a NEW session starts cleanly (proves no orphaned state). `vaultlog` analyzer passes on the new test code. Test runtime ≤ 90s. `go test ./internal/chat -run TestChat_DockerKillMidStream` passes. Closes M5.1 SC-006 + SC-009 + FR-101 carryover; SC-214.
  - **Out of scope for this task**: network-blip, Postgres-loss, pg_notify-buffer-overflow chaos cases — all deferred to M5.3 per spec FR-305. Any change to the existing M5.1 `garrison-mockclaude:m5` Dockerfile (image is reused unchanged). Any chaos beyond container-kill (FR-305 keeps the scope minimal).

---

## Phase 9 — Acceptance + retro

- [ ] T018 Acceptance: scripted check against all SC-200 → SC-222 + ops-checklist update + dependency-list verification + acceptance evidence document
  - **Depends on**: [T016](#t016), [T017](#t017)
  - **Files**: `specs/011-m5-2-ceo-chat-frontend/acceptance-evidence.md` (new — mirrors M3/M4/M5.1 pattern), `docs/ops-checklist.md` (extend with M5.2 section per plan §3.6)
  - **Completion condition**: `acceptance-evidence.md` walks every success criterion SC-200 → SC-222 with the test or evidence pinning it: SC-200 = T013 wall-clock guard; SC-201 = T013/T014 transcript ordering; SC-202 = T014 multi-session block; SC-203 = T014 end-thread block (idle path) + T015 sub-scenario `j` (mid-stream race); SC-204 = T014 archive block; SC-205 = T014 delete block; SC-206/207 = T013 cost-badge assertions; **SC-208 = T015 sub-scenario `i` (idle-pill flip after supervisor sweep with `GARRISON_CHAT_SESSION_IDLE_TIMEOUT=10s`)**; SC-209 = T015 disconnect block; SC-210 = T016 responsive block; SC-211 = T013/T014 composer-disabled assertions + T015 `j` mid-stream race; SC-212 = T011 empty-state coverage + manual visual review recorded in evidence; SC-213 = T015 cumulative-runtime guard; SC-214 = T017 chaos; SC-215 = T002 orphan-sweep test; SC-216 = T015 leak-grep block; SC-217 = T012 architecture amendment + assertion test; SC-218 = dependency verification step (single approved dev dep `@axe-core/playwright`); SC-219 = T001 migration apply; SC-220 = `vaultlog` analyzer verification; **SC-221 = T015 sub-scenario `k` (100-turn TTI < 2000ms)**; SC-222 = T015 axe-core block. Plus carryover-closure pins for M5.1: SC-006 + SC-009 + SC-010 + SC-011 + FR-101. Every SC has an automated test pin — no SC relies on manual measurement only. The full M3 + M4 + M5.1 + M5.2 Playwright suite runs and stays under 12 minutes total (re-verified in this task). `docs/ops-checklist.md` gains the M5.2 section verbatim from plan §3.6 (pre-deploy: migration apply, grant verification, FK cascade verification; post-deploy: orphan-sweep log line, sidebar entry render, `/chat/all` route load). Dependency-list verification: `git diff main..HEAD --stat -- supervisor/go.mod dashboard/package.json` shows zero supervisor changes; dashboard `package.json` shows exactly one new `devDependencies` entry (`@axe-core/playwright`). If any SC fails, this task opens a focused patch against the relevant earlier task's files, then re-runs all acceptance steps from the top — does NOT introduce new features.
  - **Out of scope for this task**: any new feature work. Any spec or plan amendment (those land in T019 retro as deferred items if needed). Any architecture amendment (T012 owns that). Any new test file (only acceptance evidence documentation).

- [ ] T019 Retro: `docs/retros/m5-2.md` + MemPalace mirror per AGENTS.md §Retros (M3-onwards dual-deliverable)
  - **Depends on**: [T018](#t018)
  - **Files**: `docs/retros/m5-2.md` (new), MemPalace `wing=garrison, room=retros, source_file=m5-2.md` drawer (mirrored via `mempalace_add_drawer` MCP tool)
  - **Completion condition**: `docs/retros/m5-2.md` covers per AGENTS.md §Retros — what shipped (chat dashboard surface + orphan-row sweep extension + activity-feed channel + Playwright golden-path with axe-core + chaos test + architecture amendment), what the spec got wrong (any deviations between FR text and implementation reality; in particular note the `/speckit-analyze` round that resulted in the FR-261 mechanism amendment, the FR-322 scoping amendment, and the eleven-sub-scenario T015 build-out for SC-208/SC-221/FR-244-race coverage), dependencies added outside the locked list (`@axe-core/playwright` dev dep with the AGENTS.md soft-rule justification), open questions deferred to the next milestone (M5.3 scope confirmations: chat-driven mutations, threat-model amendment timing; any palace-live chip refinements observed in operator-week-of-use; warm-pool optimization tracked-but-not-addressed forward-look per spec out-of-scope; deferred operator-tooling polish to wire `EventRow` rendering for the existing M5.1 `session_started`/`message_sent`/`session_ended` channels per amended FR-322), and the resolved-Q decisions log mirroring plan §Phase 5 (Q1 add axe-core, Q2 two strips, Q3 new query, Q4 no notify, Q5 string buffer). MemPalace mirror posted via `mempalace_add_drawer` (matches M5.1 mirror's convention). KG triples added via `mempalace_kg_add` for cross-milestone facts: `(M5.2, established, "ceo-chat-three-pane-dashboard-surface")`, `(M5.2, established, "use-chat-stream-hook-pattern")`, `(M5.2, established, "operator-driven-chat-session-end-archive-delete")`, `(M5.2, established, "chat-orphan-operator-row-sweep-extension")`, `(M5.2, established, "@axe-core/playwright-dev-dep-for-a11y-CI")`, `(M5.2, closes, "M5.1-T020-playwright-end-to-end-chat-test")`, `(M5.2, closes, "M5.1-chaos-m5-1-test-real-docker-kill")`, `(M5.2, defers, "M5.3-chat-driven-mutations-with-threat-model-amendment")`, `(M5.2, defers, "M5.4-knowledge-base-WHAT-THE-CEO-KNOWS-pane")`, `(M5.2, defers, "operator-tooling-polish-EventRow-rendering-for-M5.1-chat-channels")`, `(M5.2, surfaces, "cost-telemetry-blind-spot")` (still surfaced, not fixed), `(M5.2, surfaces, "workspace-sandbox-escape")` (still surfaced, not fixed). Both deliverables non-optional per AGENTS.md §Retros — the retro is not done until both have landed.
  - **Out of scope for this task**: any code changes (retro is documentation only). Any acceptance re-run (T018 owns that). Any spec or plan amendments. Any RATIONALE.md / AGENTS.md / ARCHITECTURE.md edits (T012 owned the architecture amendment).

---

## Task summary

19 tasks. Foundation (T001) → supervisor (T002) → dashboard data (T003–T005) → UI (T006–T011) → architecture (T012) → integration (T013–T016) → chaos (T017) → acceptance (T018) → retro (T019).

After every task the repo is in a working state: T001 lands schema with no app changes; T002 makes supervisor pass green with the new sweep; T003–T005 leave the data plane callable but not yet rendered; T006–T011 progressively wire up the chat surface (T006 renders the route shell; T007 lands the sidebar; T008 the header; T009 the message stream; T010 the composer; T011 the empty + error states); T012 amends the architecture line; T013–T015 close the deferred-from-M5.1 Playwright (eleven sub-scenarios a–k covering the full chat surface + the FR-244 race + the SC-208 idle-flip + the SC-221 long-thread TTI); T016/T017 close responsive + chaos; T018 walks acceptance with every SC pinned to an automated test; T019 ships retro.
